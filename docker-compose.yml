services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gymsight
      POSTGRES_PASSWORD: gymsight
      POSTGRES_DB: gymsight
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
